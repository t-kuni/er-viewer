1. 型の２重管理が嫌なのでエクスポートデータの形式はViewModelをそのまま利用する。但し一時UI状態やキャッシュについてはエクスポート時にを破棄し、インポート時にデフォルト値を入れる。
3. 案B：D&Dだけライブラリ（react-dropzone）
5. バリデーションについてはJSONがパースできること、formatが正しいこと、version >= 1 であることぐらいでOK
    * ViewModelにformatとversionを追加する
6. バージョン管理は一旦1固定でOK
7. 
    