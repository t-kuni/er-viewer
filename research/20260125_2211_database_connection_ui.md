## 1. UIデザイン（推奨と比較）

### 推奨：ハイブリッド（「通常は即実行」＋「必要時だけ接続入力」）

* **通常**：`リバースエンジニア`押下で即実行（前回成功した接続情報を使用）
* **初回/不足時/失敗時**：接続入力UIを開いて入力 → `実行`
* **接続編集**：常時見せない（頻繁に触るのは開発時だけなので「必要なときに出す」）

この形にすると、頻繁実行のUXを最大化しつつ、入力UIも自然に導線に乗ります。

### UI配置：モーダル（パターン1）ベースを推奨

* **理由**

  * 実行時に必要な情報をその場で集められる（フローが分断されない）
  * MVPで実装が最小
  * 失敗時に同じUIを再提示しやすい（エラー文言＋再入力）

### 各パターンの評価

* **パターン1: モーダル**（推奨）

  * 👍 実装小、実行フローに直結、エラー時の再入力が自然
  * 👎 開発で毎回開くと面倒 → 「前回と同じで即実行」を主導線にして解消
* **パターン2: サイドパネル**

  * 👍 いつでも編集、開発時の繰り返しに強い
  * 👎 画面常時占有、MVPでUI負債になりやすい（React Flow領域が狭くなる）
* **パターン3: インライン展開**

  * 👍 操作が軽い、視線移動が少ない
  * 👎 画面が散らかりやすい／レイアウト崩れ要因
* **パターン4: 設定画面**

  * 👍 管理が整理される
  * 👎 MVPで導線が遠い（実行→設定→戻る）

### モーダル内UI案（最小）

* フィールド：Host / Port / User / Password / DB Name（MySQL固定表示）
* ボタン：

  * `実行`（主）
  * `キャンセル`
  * （任意）`接続テスト`はMVPでは省略推奨（後述）
* デフォルトの埋め方（UI表示値の優先順位）
  **前回成功（ViewModel保持） > サーバ既定（環境変数） > ハードコード**
* パスワードは

  * 入力欄は常に空（マスク）
  * 可能なら「このタブで保持（SessionStorage）」チェック（任意）

---

## 2. データモデル設計（TypeSpec案）

### 推奨：`settings`を新設して「アプリ設定」として分離（パターン3寄り）

* **理由**

  * `ui`は純UI状態なので、接続情報（実行に影響する設定）を混ぜない
  * `erDiagram`も本体データなので混ぜない
  * 将来PostgreSQLやSSL等を足す場所が明確

### パスワードはViewModelに入れない（推奨）

* 成功時に保持するのは **パスワードを除いた接続情報**（次回の入力補助用）

#### TypeSpec例

```typespec
enum DatabaseType {
  mysql,
  postgresql,
}

// 実行時にサーバへ送る入力（passwordを含みうる）
model DatabaseConnectionInput {
  type: DatabaseType;          // MVPは mysql 固定でOK
  host?: string;
  port?: int32;
  user?: string;
  password?: string;
  database?: string;
}

// ViewModelに保持する状態（passwordは保持しない）
model DatabaseConnectionState {
  type: DatabaseType;
  host: string;
  port: int32;
  user: string;
  database: string;
}

model AppSettings {
  lastDatabaseConnection?: DatabaseConnectionState; // リバースエンジニア成功時に更新
}

model ViewModel {
  format: string;
  version: int32;
  erDiagram: ERDiagramViewModel;
  ui: GlobalUIState;
  buildInfo: BuildInfoState;

  settings?: AppSettings;
}
```

---

## 3. API設計（推奨）

### 推奨：リクエストを分ける（パターン2）

* **理由**

  * passwordをViewModelに混ぜない設計を保てる
  * 「実行入力」と「保存状態」を分離できる
  * 将来の拡張（接続テスト、DB種別追加）も素直

#### TypeSpec例

```typespec
model ReverseEngineerRequest {
  viewModel: ViewModel;
  connection?: DatabaseConnectionInput; // 未指定ならサーバ側既定で解決
}

@route("/api")
namespace API {
  @post
  @route("/reverse-engineer")
  op reverseEngineer(@body request: ReverseEngineerRequest): ViewModel | ErrorResponse;
}
```

### バックエンドでの接続情報解決（実行時優先順位）

**request.connection（明示入力） > viewModel.settings.lastDatabaseConnection > 環境変数 > ハードコード**

※UIの「前回と同じで実行」は、`connection`を省略しても成立します（サーバがViewModel保持値を使う）。

---

## 4. パスワードの扱い（方針と選択肢）

### 推奨方針：ViewModelに保存しない

* **理由（あなたの前提に直結）**

  * ViewModelがエクスポート/保存対象 → 平文混入事故が起きやすい
  * MVPでも「消し忘れ」事故が起こりやすい（後で消す運用は漏れる）

### 代替案（必要なら）

* **案A：SessionStorageにのみ保持（任意チェック）**

  * 👍 開発の繰り返しが楽、ファイルには出ない
  * 👎 同一端末の同一ブラウザタブ前提、完全に安全ではない
* **案B：パスワードはサーバ環境変数のみ**

  * 👍 UI入力不要・漏洩面が小さい
  * 👎 接続先をUIで変えたい用途と相性が悪い（ユーザ/DBだけ変える等も制限）

MVPの整合としては

* **普段：サーバ環境変数のパスワード**
* **例外：UI入力（送るだけ、保持しない）**
  が一番破綻しにくいです。

---

## 5. 実装への影響範囲（変更点リスト＋工数感）

### フロントエンド

* 追加

  * 接続入力モーダル（component）
  * 状態：`settings.lastDatabaseConnection` を編集するAction/Reducer（ViewModel更新に含めてもよい）
  * （任意）SessionStorageにpassword保持する処理
* 変更

  * `public/src/commands/reverseEngineerCommand.ts`

    * 現行：ViewModelだけ送信
    * 変更：`ReverseEngineerRequest`を送信（必要時のみ `connection` を同梱）
  * TypeSpec生成物（クライアント型/DefaultService）の更新

### バックエンド

* 変更

  * `scheme/main.tsp`：`ReverseEngineerRequest`追加、opシグネチャ変更
  * `lib/usecases/ReverseEngineerUsecase.ts`

    * 引数を `(request)` に変更
    * 成功時に `viewModel.settings.lastDatabaseConnection` を更新して返す
  * `lib/database.ts`

    * `connect(config?: DatabaseConfig)` のように外部入力を受け取れるよう変更
    * 「環境変数/ハードコード」フォールバックの責務を `resolveConnection()` に寄せる

### インポート・エクスポート

* `settings.lastDatabaseConnection`が含まれるようになる
* passwordを入れない前提なら警告UIは不要（入れる場合だけ警告が必要）

### 工数感（MVP）

* **最小（モーダル＋request分離＋password非保持）**：1〜2日程度
* **＋SessionStorage保持オプション**：+0.5日
* **＋接続テストAPI/ボタン**：+1日（DB接続→即切断、エラー整形、UI反映）

---

## 6. UX（頻繁実行を最優先したフロー）

### 推奨フロー

1. `リバースエンジニア`押下
2. サーバ側で接続情報を解決して実行

   * `lastDatabaseConnection`があればそれを使用
   * なければ環境変数（＋ハードコード）で実行
3. **失敗時のみ**モーダルを開く

   * エラー文言表示（例：認証失敗/ホスト到達不可/DBなし）
   * 入力して再実行

### ショートカット

* `前回と同じで実行` を実装するなら、ボタンは分けずに

  * 通常の `リバースエンジニア` がそれ（最短導線）
  * 近くに `接続…`（または歯車）でモーダルを開く
    がUI的に軽いです。

---

## 7. 将来の拡張性（PostgreSQL）

### いま仕込むべき最低限

* `DatabaseType` を型として持つ（UIは当面固定表示でもOK）
* `port` のデフォルトをtypeで切替可能にする（mysql=3306, postgresql=5432）
* `DatabaseManager`は

  * `createDatabaseManager(type)` で実装差し替え
  * `generateERData()` を共通IFにする

### PostgreSQL対応時の変更範囲

* TypeSpec：`DatabaseType`の利用をUIで解放（ドロップダウン表示）
* バックエンド：`DatabaseManager`のPostgreSQL実装追加、依存注入切替
* UI：type変更時にportの既定値を自動調整（未編集の場合のみ）

---

## 8. 最終推奨案（要求8点に対応）

1. **UIデザイン**：モーダル（必要時のみ表示）＋通常は即実行
2. **データモデル**：`ViewModel.settings.lastDatabaseConnection`（passwordなし）
3. **API設計**：`ReverseEngineerRequest { viewModel, connection? }`（passwordはconnection側のみ）
4. **パスワード**：ViewModel保存しない（必要ならSessionStorage任意）
5. **優先順位**：`request.connection > ViewModel(last) > env > hardcoded`
6. **実装ステップ**

   * (1) TypeSpec変更（settings追加、ReverseEngineerRequest導入）
   * (2) バックエンド：接続解決関数＋DatabaseManagerに外部config注入
   * (3) フロント：モーダル実装＋ReverseEngineerRequest送信
   * (4) 成功時：lastDatabaseConnection更新（password除外）
   * (5) 失敗時：エラー表示→モーダル誘導（再実行）
   * (6)（任意）SessionStorage保持、接続テスト追加
