import puppeteer from 'puppeteer';
(async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); await page.goto('http://localhost:30033'); await page.waitForSelector('#er-canvas'); await page.evaluate(() => { const canvas = document.getElementById('er-canvas'); const event = new WheelEvent('wheel', { deltaY: 100, bubbles: true }); canvas.dispatchEvent(event); }); await page.screenshot({ path: 'after-scroll.png' }); await browser.close(); })();
